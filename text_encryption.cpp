#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

    int case_cnt;
    int cur_pos=0;
    scanf("%d", case_cnt);

    while(cur_pos<case_cnt){
        char key[100];
        char plain[100];
        scanf("%s", key);
        scanf("%s", plain);

        int order[100];

        for(int i=0;i<100;i++)
            order[i] = i;

        for(int i=0;i<strlen(key);i++){

        }


        cur_pos++;
    }

    return 0;
}
